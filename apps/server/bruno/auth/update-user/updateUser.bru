meta {
  name: updateUser
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/auth/update-user
  body: json
  auth: inherit
}

body:json {
  {
    "name": "",
    "image": ""
  }
}

headers {
  Origin: http://localhost:3001
  Cookie: {{authCookie}}
}
