meta {
  name: changePassword
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "newPassword": "",
    "currentPassword": "",
    "revokeOtherSessions": false
  }
}

headers {
  Origin: http://localhost:3001
  Cookie: {{authCookie}}
}
