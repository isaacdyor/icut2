meta {
  name: Revoke a single session
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/auth/revoke-session
  body: json
  auth: inherit
}

body:json {
  {
    "token": ""
  }
}

headers {
  Origin: http://localhost:3001
  Cookie: {{authCookie}}
}
