meta {
  name: project.update
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/projects/:id
  body: json
  auth: inherit
}

body:json {
  {
    "name": ""
  }
}

headers {
  Origin: http://localhost:3001
}
