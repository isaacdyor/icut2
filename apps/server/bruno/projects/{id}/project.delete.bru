meta {
  name: project.delete
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/projects/:id
  body: json
  auth: inherit
}

body:json {
  {}
}

headers {
  Origin: http://localhost:3001
  Cookie: {{authCookie}}
}
